spring:
  application:
    name: userservice #服务名
  profiles:
    active: dev #开发环境
  cloud:
    nacos:
      server-addr: localhost:8848 #nacos端口地址
#      server-addr: nacos:8848 #nacos端口地址
      config:
        file-extension: yaml  #文件后缀名
